# Charging behavior matrix

This is the complete, authoritative description of **what the brain does in every
situation** — for each charging mode, given the price, the home-battery state and the
solar surplus. It is generated by hand from the regulation engine
([`engine.py`](../custom_components/goe_steve/engine.py), function `decide()`) and is the
reference for "why did it do *that*?".

Everything the engine outputs is a single `Decision`:

- **control** — `false` means the brain keeps its hands off entirely (you keep manual /
  go-e-app control); `true` means it is actively driving the charger.
- **should_charge** + **target current (A)** + **target phases** — the actual command.
- **hold_battery** — `true` flips your mapped "stop discharge" switch on so the car draws
  from the grid, not the home battery (only ever set while grid-charging).
- **reason** — the plain-language string shown on the *Status* sensor and the card.

Default tunables referenced below (all runtime-adjustable):

| Tunable | Default | Meaning |
|---|---|---|
| Min / Max current | 6 A / 16 A | charge-current bounds |
| Cheap price | 0.15 /kWh | at/below this, grid counts as "cheap" |
| Home battery reserve (Protect) | 80 % | car waits until the home battery reaches this |
| Home battery floor (Assist) | 20 % | stop draining the home battery here |
| Minimum charge power (PV+min) | 1400 W | floor that mode always delivers |
| Car target energy / departure | 0 / unset | enables cheapest-hours deadline planning |

---

## 1. Master gates — checked first, in this order, for every mode

These short-circuit before any mode logic runs.

| # | Condition | control | Charges? | Reason | Notes |
|---|---|---|---|---|---|
| 1 | **Smart control** switch is **off** | `false` | — | *Smart control disabled* | Full manual control returns. |
| 2 | Mode = **Off** | `false` | — | *Mode: Off — manual control* | Brain hands off; price/battery/PV irrelevant. |
| 3 | **No car connected** | `true` | no | *No car connected* | Brain holds the charger off. |

If none of these fire, the per-mode logic below applies.

---

## 2. Mode overview — what each mode reacts to

| Mode | Solar surplus | Cheap grid (price ≤ threshold) | Deadline plan (target + departure) | Grid top-up |
|---|---|---|---|---|
| **Off** | — | — | — | — (manual) |
| **Solar surplus only** (`pv_only`) | ✅ charges on surplus | ✖ ignores price | ✖ | ✖ never |
| **Solar + minimum** (`pv_minimum`) | ✅ charges on surplus | ✖ ignores price | ✖ | ✅ always tops up to the floor |
| **Solar + cheap grid** (`pv_price`) | ✅ charges on surplus | ✅ full power when cheap | ✖ | ✅ only while cheap |
| **Price-optimized** (`price`) | ✖ surplus ignored | ✖ (uses plan, not threshold) | ✅ charges in cheapest hours | ✅ in planned hours |
| **Combined** (`combined`) | ✅ charges on surplus | ✅ full power when cheap | ✅ + cheapest-hours guarantee | ✅ when cheap or planned |
| **Fast** (`fast`) | — | — | — | ✅ always at max |

---

## 3. Per-mode decision matrix

Each row is a situation; the result is what the engine commands. "Full phases" =
`max_phases` when Auto phase is on, otherwise the charger's current phase count (see §6).

### Fast
| Situation | Charges? | Current | Phases | hold_battery | Reason |
|---|---|---|---|---|---|
| Car connected | ✅ | **Max** | Full | per policy (§5) | *Fast charging at {amps} A* |

Ignores price, PV and SoC entirely — it just charges at full power.

### Solar surplus only (`pv_only`)
| Situation | Charges? | Current | Phases | Reason |
|---|---|---|---|---|
| Surplus ≥ min current | ✅ | surplus, clamped [min, max] | adaptive (prefers 1φ) | *Solar surplus {W} → {A}* |
| Surplus < min, **Protect** holding (SoC < reserve) | ✖ | 0 | — | *Waiting — home battery {soc}% < reserve {reserve}%* |
| Surplus < min, otherwise | ✖ | 0 | — | *Waiting for surplus — {W} < {needed} W* |

Never pulls from the grid. Price is irrelevant.

### Solar + minimum (`pv_minimum`)
| Situation | Charges? | Current | Phases | Reason |
|---|---|---|---|---|
| Surplus covers the floor | ✅ | surplus, clamped [min, max] | adaptive | *Solar surplus {W} → {A}* |
| Surplus below the floor | ✅ | floor (≥ Minimum charge power), from grid | adaptive | *Minimum {A} (surplus only {W}, topping up from grid)* |

Always charges — it never "waits". The grid top-up here does **not** engage the
battery-hold switch (it is the surplus branch, not a grid-charging branch), so under
Protect/Share the home battery can still cover part of the floor. Use *Solar + cheap grid*
or a hold switch if that matters.

### Solar + cheap grid (`pv_price`)
| Situation | Charges? | Current | Phases | hold_battery | Reason |
|---|---|---|---|---|---|
| **price now ≤ cheap price** | ✅ | **Max** (trimmed by battery guard, §5) | Full | per policy | *Cheap grid {price}/kWh ≤ {threshold} → full power* |
| price above threshold | falls through → behaves exactly like **Solar surplus only** | | | | |

### Price-optimized (`price`)
Needs **Car target energy > 0**, a **departure** time and a **price forecast**. The engine
runs a greedy cheapest-hours plan every cycle: it picks the cheapest upcoming slots until
their capacity covers the target, and charges only when *now* is one of them.

| Situation | Charges? | Current | Phases | hold_battery | Reason |
|---|---|---|---|---|---|
| Now is in the cheapest-hours set | ✅ | **Max** (battery-guarded, §5) | Full | per policy | *Cheap-hours plan: charging now at {price}/kWh to reach {target} kWh by departure* |
| Now is not a planned hour | ✖ | 0 | unchanged | — | *Waiting for a cheaper price window* |
| No target / departure / forecast | ✖ | 0 | unchanged | — | *Waiting for a cheaper price window* |

Solar surplus is **ignored** in this mode — it charges purely on the price plan.

### Combined (`combined`)
The union of cheap-grid, deadline planning and solar surplus, evaluated in that priority:

| Situation (first match wins) | Charges? | Current | Phases | hold_battery | Reason |
|---|---|---|---|---|---|
| price now ≤ cheap price | ✅ | **Max** (guarded, §5) | Full | per policy | *Cheap grid … → full power* |
| now is in the cheapest-hours plan | ✅ | **Max** (guarded, §5) | Full | per policy | *Cheap-hours plan: …* |
| otherwise → solar surplus | as **Solar surplus only** above | | | | |

---

## 4. Anti-flap dwell (applies to every charging decision)

Once the engine starts or stops charging it holds that state for a minimum dwell
(default 120 s on, 120 s off) before flipping. While the timer blocks a change you'll see
*Holding charge (anti-flap dwell)* or *Holding off (anti-flap dwell)* instead of the
underlying reason, and the previous on/off state is kept.

---

## 5. Battery policy × what the car may take

The battery policy shapes **how much surplus** the car sees (solar charging) and **whether
the home battery is blocked** (grid charging).

### Solar surplus available to the car (`compute_surplus`)
| Policy | Home battery below its threshold | Home battery above its threshold |
|---|---|---|
| **Protect** | surplus = **0** (car waits, battery fills first to *reserve*) | real PV export only; battery discharge subtracted (car never runs on the battery) |
| **Share** | reclaims power that would charge the battery; battery discharge subtracted (never actively drained) | same |
| **Assist** | reclaims battery-charge power | above *floor* SoC, availability is lifted to **Max** so charging never stalls — the battery and grid floor make up the difference |

### Battery-hold during grid charging (`hold_battery` / `_battery_guard_current`)
Only set on the grid-charging branches (Fast, cheap-grid, deadline). It blocks the home
battery from quietly covering the car's grid load. If you mapped a **hold switch**, the
brain flips it on; if not, it falls back to *trimming* the car's current by whatever the
battery is discharging (shown as "protecting home battery → {A}").

| Policy | Hold the battery? |
|---|---|
| **Protect** | **Always** (battery never powers the car) |
| **Share** | Hold when SoC ≤ **reserve**; above reserve, let it help |
| **Assist** | Hold when SoC ≤ **floor**; above floor, it is meant to assist |
| any, **SoC unknown** | Protect/Share hold (safe default); Assist does not hold |

---

## 6. Phase selection (`auto_phase`)

| Auto phase | Charging type | Phases used |
|---|---|---|
| **Off** (or no 3φ headroom / no phase entity mapped) | any | charger's current phase count, untouched |
| **On** | grid charging (Fast / cheap-grid / deadline) and Price mode | **Max phases** (full power) |
| **On** | solar surplus | adaptive **1 ↔ 3φ** with hysteresis + a dwell timer; **prefers 1φ** so a small surplus still charges, climbing to 3φ once the surplus sustains the 3φ minimum |

Hysteresis band: switch **up** to 3φ once surplus ≥ `min_current × 3 × V`; switch **back**
to 1φ once surplus < `max_current × 1 × V`. A dwell timer (default 300 s) caps how often
the contactor may toggle.

---

## 7. Safety fall-throughs

- Car disconnected or **Smart control off** → brain stops driving the charger (§1).
- Stale / missing required data → the coordinator keeps the brain's hands off (control
  stays effectively manual) rather than acting on bad numbers.
- Sub-amp changes are not rewritten to the charger (relay protection, cleaner logs).
